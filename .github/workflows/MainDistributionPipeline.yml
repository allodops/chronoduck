#
# This workflow calls the main distribution pipeline from DuckDB to build, test and (optionally) release the extension
#
name: Main Extension Distribution Pipeline
on:
  push:
  pull_request:
  merge_group:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ github.head_ref || '' }}-${{ github.base_ref || '' }}-${{ github.ref != 'refs/heads/main' && github.sha || '' }}
  cancel-in-progress: true

jobs:
  duckdb-stable-build:
    name: Build extension binaries
    uses: duckdb/extension-ci-tools/.github/workflows/_extension_distribution.yml@v1.5-variegata
    with:
      duckdb_version: v1.5.4
      ci_tools_version: v1.5-variegata
      extension_name: chronoduck

  code-quality-check:
    name: Code Quality Check
    uses: duckdb/extension-ci-tools/.github/workflows/_extension_code_quality.yml@v1.5-variegata
    with:
      duckdb_version: v1.5.4
      ci_tools_version: v1.5-variegata
      extension_name: chronoduck
      format_checks: 'format;tidy'

  # No deploy job: `_extension_deploy.yml` uploads to the `duckdb-extensions-nightly` S3
  # bucket using secrets (DUCKDB_EXTENSIONS_NIGHTLY_S3_*) that GitHub only shares with
  # reusable-workflow callers inside the same org as the secret owner — this repo isn't
  # in the duckdb org, so calling it would fail on every run regardless of dry-run
  # settings, not just be inert. `duckdb-stable-build` above already proves the extension
  # builds and packages correctly (uploaded as a GH Actions artifact); that's this
  # project's dry-run signal until a real distribution channel (community-extensions,
  # T0.11) is wired up.
