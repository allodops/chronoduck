Closes #124

## How
Added scripts/pr-label.mjs (event mode driven by PR number, backfill mode walking every open
PR) plus a matching workflow, mirroring the size:/area: labels already present on the linked
issue onto the PR.

## Deviations
None.

## Risk
Low — additive only, never removes a label a human added by hand.

## Evidence
- Ran `bun scripts/pr-hygiene.mjs 125` locally against the live PR: PASS.
- Verified the Epic exemption against the live API.

## Discovered
None.

Constitution check: none (no change to CONSTITUTION.md)
