@AGENTS.md

# Claude-specific notes

- Use `gh-tsouza`, never plain `gh`, for every GitHub CLI call in this project.
- When reviewing a PR per `.claude/rules/review.md`, do it in a fresh session with only the PR number and `CONSTITUTION.md` as context — no memory of the implementing session.
